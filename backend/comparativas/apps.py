from django.apps import AppConfig


class ComparativasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "comparativas"
    verbose_name = "Comparativas de presupuestos"
